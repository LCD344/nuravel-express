const faker = require('faker');
const {{model}} = require('../../app/models/{{model}}');
const Factory = require('nuravel-express').Factory;

class {{name}} extends Factory {
	model() {
		return {{model}};
	}

	define() {
		return {
		}
	}
};

module.exports = new {{name}}();
